import React from 'react';

export const Footer = (props) => {
    return (
        <footer className="container-fluid">
            <p>&copy; {new Date().getFullYear()} Copyright BIT</p>
        </footer>

    );
}
